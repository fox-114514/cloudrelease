2026-6-21
1.安卓端:登陆管理时中途如果输错信息,该页面直接死掉,即使更正之后登录按钮也亮不起来,只能返回重新进入,之前填写的内容全部丢失,不方便我不知道其他需要填写信息的页面是不是也是这样,需要排查;
2.安卓端:管理页面生成绑定码后,绑定码应持续显示方便复制而不是在一个不可复制且过几秒就消失的小浮窗,或者直接写入粘贴板并提示;
3.存在未知问题:绑定失败:NetworkOnMainThreadException
4.后端:对已撤销设备的管理:无法删除已撤销设备,持续占用设备名,不便管理

0.4.0 修复状态（2026-6-21）
1. 已修复：所有登录/绑定/生成绑定码操作在失败后都会恢复按钮，可直接修改内容重试。
2. 已修复：生成的绑定码持续显示、支持选择复制，并自动复制到剪贴板。
3. 已修复：Android 网络请求统一在 IO 调度器执行，不再触发 NetworkOnMainThreadException。
4. 已修复：后端新增撤销设备软删除接口，Android 与网页后台均可删除并保留历史记录。
部署状态：64.90.30.102:3000 后端已确认更新为 0.4.0。

0.4.1 补充修复（2026-6-21）
Android 0.4.0 会把大小写敏感的绑定码强制转换为大写，导致新绑定码被后端判定为无效。0.4.1 保留绑定码原始大小写，并允许复制内容带首尾空白。

0.4.2 补充修复（2026-6-21）
实时模式此前只负责监听，上传仍完全交给 WorkManager；Android 进入后台后任务会被 JobScheduler 批处理，出现十秒以上随机延迟。0.4.2 改为前台服务直接上传，WorkManager 仅作为断网和进程退出后的可靠兜底。

0.4.3 补充修复（2026-6-21）
连接 OPD2508 实机复测确认：ColorOS 在 StudyShot 处于后台时会延迟 MediaStore ContentObserver 回调。实时模式新增每秒一次的最近媒体主动扫描，与事件监听并行，避免等待厂商延迟回调。
2026-6-22
1.子用户逻辑错误:子用户应该拥有一定的管理权限:1.可以生成设备码(自动指向该子用户),现有逻辑下无论如何子用户都不能生成设备码;2.子用户可以修改名下设备的权限,新绑定设备的范围应该默认为same_user_only,天然拥有手动上传和手动下载权限;顺带检查并修复相关问题.
2.手动下载功能缺失:即使某些设备拥有手动下载功能,但是子用户无法查看到当前图片库.即使是owner用户,能看到所有它图片,但是依然没有下载途径.电脑端后台可以通过浏览器原生功能另存图片,客户端无相应功能.另外安卓端查看图片库无法正常加载图片.
3.存在bug:windows端改完图片存放位置后，截屏发送电脑上时仍然会使用默认位置
*重点*完善用户逻辑,修复功能,修复bug

2026-6-22 修复状态
1. 已修复子用户设备逻辑：子用户可生成自动指向自己的绑定码，可管理本人设备的安全用途预设及手动上传/下载权限；未指定用途的新子用户设备默认 same_user_only，并默认拥有手动上传、手动下载权限。跨用户设备、跨成员来源及管理空间等高危权限仍禁止越权。
2. 已补全手动下载：拥有 canManualDownload 的设备可列出并下载本人图片；Android 图库可使用账号或设备权限加载预览并通过系统“另存为”保存；Windows/Linux 客户端及网页图库增加明确下载入口，文件名统一为“上传设备名_上传时间”。
3. 已修复 Windows 下载目录：目录选择完成后立即持久化并通知接收客户端，后续图片使用新目录，不再被异步状态刷新覆盖回默认目录。
验证状态：后端、Windows、Linux 均通过 TypeScript 构建；Android compileDebugKotlin 成功；Linux 2 项测试和后端 5 项无数据库单元测试通过。后端数据库集成测试需本机 localhost:55432 PostgreSQL，当前环境未启动该服务。
